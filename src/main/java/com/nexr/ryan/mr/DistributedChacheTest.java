package com.nexr.ryan.mr;

import org.apache.hadoop.conf.Configuration;

public class DistributedChacheTest {
	public static void main(String[] args) throws Exception {
		if (args.length != 3) {
			System.exit(2);
		}
		
		Configuration conf = new Configuration();
	}
}
